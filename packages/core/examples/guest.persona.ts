import { Persona } from '../common/mixin/persona.mixin';

export const GuestPersona = new Persona({
  name: 'Guest User',
  story:
    'I am a new user who has landed on the site for the first time from Google. I have not yet created an account.',
});
